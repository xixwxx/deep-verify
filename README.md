# deep-verify

> Claude(Anthropic) + Codex(OpenAI)를 같이 쓰면서, **"한 모델만 보면 놓치는 걸 다른 회사 모델·다른 관점은 잡더라"**는 경험에서 만든 Claude Code 검증 스킬.

코드 변경을 머지하기 직전에, **세 방향(실제 테스트 실행 + 다른 회사 모델 교차검증 + 다관점 적대 리뷰)**으로 동시에 검증하고, 정해진 규칙으로 합쳐 **"확실히 확인된 것 / 의견뿐인 것"을 절대 섞지 않고** 보고합니다.

## 왜 만들었나

저는 Claude랑 Codex를 같이 쓰는데 — 한쪽(Claude **또는** Codex)에만 매몰되면, **같은 모델이 같은 걸 또 봐도 관점이 같아서** 그 모델이 못 잡는 버그·에러는 계속 못 잡더라고요. 그런데 **다른 회사 모델이, 다른 관점에서 보면** 보이는 버그·에러가 있었어요.

원래는 **Claude ↔ Codex 둘만 교차검증**하는 스킬을 쓰고 있었는데([cross_verify](https://github.com/xixwxx/cross_verify)), 거기서 한 발 더 나가 **다관점(adversarial) + Codex + Claude의 의견을 한 번에 합쳐서 찾는** 스킬을 만든 게 이 `deep-verify`입니다.

개인적으로 편하게 쓰고 있어서 공개합니다.

## 어떻게 작동하나

변경분/구현을 **세 레인으로 동시에** 검증하고, **고정된 결정 규칙**으로 합칩니다:

1. **결정적·실행** — 테스트·빌드를 *말로만 말고 실제로 돌려서* 통과/실패를 **사실**로 캡처
2. **교차검증** — **다른 회사 모델(Codex)**에게 read-only 코드 리뷰 (같은 회사 모델은 나와 같은 사각지대를 공유하니 의미가 적음)
3. **다관점 적대** — 여러 렌즈(정확성·보안·재현·계약·경계)로 나눠 *"틀렸다고 증명해봐"*

→ 그리고 제일 중요한 규칙: **"체크할 게 없으면 절대 초록불(통과)로 안 칠한다."** **초록불 ≠ 참.** 결정적으로 확인된 것과 의견뿐인 것을 두 칸으로 나눠 보고합니다.

## 언제 쓰면 좋나

모든 작업에 의심을 갖는 건 아니에요. 다만 **"뭔가 확인해야 직성이 풀리는 성격"**이라면 — 공들인 코드를 머지/배포하기 직전, 틀리면 곤란한 데 도움이 될 거예요. (오타·한 줄 같은 작은 수정엔 과합니다.)

## 설치

```bash
git clone https://github.com/xixwxx/deep-verify ~/.claude/skills/deep-verify
```
→ Claude Code 새 세션에서 **"딥검증"** 또는 **"deep-verify"**라고 하면 발동합니다.
(또는 이 repo를 받아 `deep-verify` 폴더를 `~/.claude/skills/` 안에 두어도 됩니다.)

## 필요한 것

- **Claude Code**
- **Codex CLI** — 교차검증 레인(②)용. 없으면 그 레인은 건너뛰고 ①③만 돌아갑니다.

## 같이 쓰면 좋은 것

저는 이 스킬에 더해, **무엇을 작업했고 / 무엇을 찾았고 / 어떤 걸 어떻게 고쳤나를 보고하는 스킬**도 걸어놓고 함께 씁니다. 검증 → 수정 → 보고가 한 흐름으로 남아서 편해요.

## 피드백 환영 🙏

Claude·Codex를 쓴 지 얼마 안 돼서 부족한 점이 많습니다. **고칠 점이나 알려주실 점이 있으면 꼭 말씀해 주세요** — 배우면서 다듬고 싶어요. Issue·PR 환영합니다.
